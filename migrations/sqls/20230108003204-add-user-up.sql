/* Replace with your SQL commands */

CREATE TABLE users(
    id serial primary key,
    name varchar(50),
    email varchar(50),
    firstName varchar(50),
    lastName varchar(50),
    password varchar(50)
);